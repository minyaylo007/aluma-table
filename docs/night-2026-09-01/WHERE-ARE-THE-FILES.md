# Нічний прогін 2026-09-01/02 — де важкі файли

Тут лишилися лише звіти (`.md`), `reports/` і `meta/` — приблизно 2 МБ.

> **Замасковано 12.09.2026, перед публікацією репозиторію:** номер облікового запису AWS у назвах
> бакетів замінений на `000000000000`, тож команди нижче без справжньої назви не запрацюють.
> Оригінал — у приватному архіві `aluma-table-archive-2026-09`.

Важке (research 919 МБ, imagegen 450 МБ, screens 22 МБ) — у S3 Deep Archive:

    s3://gosha-archive-000000000000/night-2026-09-01/

1918 об'єктів, 1.46 ГБ, вивантажено й звірено 2026-09-10. Бакет **приватний**
(block public access увімкнено повністю, SSE-AES256). Навмисно НЕ бакет сайту
`fx-table-site-000000000000`: той є default-origin CloudFront-роздачі, тобто
все, що в ньому лежить, читається з `https://table.central-aparts.store/<ключ>`.

Дістати (чекати ~12 годин на відновлення з Deep Archive):

    aws s3 cp --recursive s3://gosha-archive-000000000000/night-2026-09-01/ . --force-glacier-transfer

## Чого тут немає навмисно

`CREDENTIALS.md` (пароль адмінки table.central-aparts.store і `IP_SALT`) не
потрапив ані сюди, ані в архів. Він лежить у `~/Desktop/gosha-private/night-CREDENTIALS.md`,
поза будь-яким репозиторієм.
